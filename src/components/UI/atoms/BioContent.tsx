import styled from "styled-components";

const BioContents = styled.span``;

const BioContent: React.FC = () => {
  return (
    <>
      <BioContents></BioContents>
    </>
  );
};

export default BioContent;
